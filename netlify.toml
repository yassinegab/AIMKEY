# Next.js sur Netlify : le runtime Next est détecté automatiquement quand le dépôt contient next.
# https://docs.netlify.com/frameworks/next-js/overview/

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"
